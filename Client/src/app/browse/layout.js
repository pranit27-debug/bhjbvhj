export const metadata = {
  title: 'Browse Items - RentEase',
  description: 'Discover available rental items in your area - furniture, appliances, bikes, tools, electronics, and sports equipment',
};

export default function BrowseLayout({ children }) {
  return children;
}
